# Prompt 1 — .gitignore

## Prompts
- "create a gitignore to this project"
- "a gitignore to the frontend"

## Ações
- Criado `frontend/.gitignore` ignorando `node_modules/`, `build/`, `coverage/`, arquivos `.env.local` e logs.
